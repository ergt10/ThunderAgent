capsule_version: 1
handoff_id: "skyrl-thunderagent-harbor-project-2026-03-22"
generated_at: "2026-03-22T01:09:55-07:00"
author: "Codex"
scope: "Top-level project state for the ThunderAgent-in-SkyRL proving-ground effort. Execution details for the Harbor benchmark live in a separate structured run spec."

project:
  name: "SkyRL ThunderAgent Harbor Use Case"
  repo_path: "/home/hkang/zthunder_yagent/SkyRL"
  objective: "Show that adding ThunderAgent as the rollout gateway to SkyRL + Harbor fully-async training creates a valuable use case by reducing training-step wall-clock and enabling more efficient RL experimentation."
  success_criteria:
    - "Produce a stable, reproducible no-ThunderAgent baseline and a comparable ThunderAgent treatment on Harbor + SkyRL + fully-async + R2EGYM."
    - "Capture trustworthy 1-step total-time measurements that isolate the effect of the ThunderAgent gateway."
    - "Preserve readable replay artifacts and monitoring so future agents can reproduce, extend, and audit the comparison."
  non_goals:
    - "Do not prioritize final model accuracy before the 1-step benchmark path is stable."
    - "Do not treat historical runbooks as the only source of truth when code or current handoff differs."
    - "Do not broaden claims beyond the Harbor + R2EGYM fully-async use case yet."
  code_ref:
    branch_or_commit: "ergt10/skyrl-ta-runtime-full @ 6344f2b74b9476a0310eceed21f363affcd02733"
    dirty_state: "dirty_untracked_execution_layer_files"

current_state:
  phase: "debugging"
  status: "ready_to_continue"
  summary: |
    The repo already contains ThunderAgent-aware Harbor entrypoints, integration docs, and replay assets.
    The active project bottleneck is experiment orchestration stability, not missing core integration code.
    Current work is focused on making the Harbor benchmark path reproducible, readable, observable, and cheap to recover when a stage fails.
    Near-term success is a trustworthy 1-step baseline vs ThunderAgent timing comparison.
  last_known_good_checkpoint: "The custom cross-job Harbor wrapper on disk now exposes prepare/head/ray/rollout/driver/status/cleanup-stage lifecycle boundaries instead of a single all-or-nothing launch path."
  next_safe_resume_point: "Read the Harbor benchmark run spec, validate the stage-based orchestration path on the current allocation, then run the no-ThunderAgent baseline and ThunderAgent treatment under the same artifact contract."

constraints:
  invariants:
    - "Never scancel parent Slurm jobs during experiment cleanup."
    - "Minimize failure cost; recover at the smallest stage boundary that preserves correctness."
    - "Protect fairness of the ThunderAgent vs no-ThunderAgent comparison; any runtime difference must be deliberate and documented."
    - "Use code plus current handoff/replay docs as source of truth; historical runbooks are references, not authority."
  permissions:
    - "Allowed: improve wrapper, handoff, replay, and monitoring layers that increase reproducibility and observability."
    - "Allowed: clean up only targeted child steps or explicitly owned helper processes."
    - "Forbidden: let cluster-specific execution fixes silently redefine the benchmark contract."
  assumptions:
    - "The current primary benchmark metric is total wall-clock for 1 training step."
    - "Harbor + SkyRL + fully-async + R2EGYM remains the chosen proving ground for ThunderAgent value."
    - "The current dirty workspace state is limited to execution-layer files: handoff docs, a replay runbook, and the custom cross-job wrapper."

active_workstreams:
  - id: "ws_integration_correctness"
    name: "TA-SkyRL-Harbor integration correctness"
    owner: "hkang + agents"
    status: "active"
    priority: "high"
    goal: "Keep the ThunderAgent integration semantically correct so any measured runtime gain is believable."
    latest_truth: "ThunderAgent-aware Harbor fully-async entrypoints and weight-sync coordination design docs are present in the branch; live benchmark validation is the remaining correctness pressure."
    blockers:
      - "Timing claims are not yet trustworthy until the experiment path is stable enough to compare baseline and treatment cleanly."
    next_action: "Preserve current integration semantics while the benchmark path is stabilized."
    child_capsule: null
    evidence: ["E2", "E3"]
  - id: "ws_orchestration_monitoring"
    name: "Experiment orchestration and monitoring"
    owner: "hkang + agents"
    status: "active"
    priority: "high"
    goal: "Run the Harbor benchmark with readable artifacts, stage-local recovery, and minimal restart blast radius."
    latest_truth: "A custom cross-job wrapper now exists to support stage-based lifecycle control and targeted cleanup on the current cluster shape."
    blockers:
      - "The stage-refactored wrapper still needs fresh dynamic validation after the latest lifecycle changes."
    next_action: "Smoke-validate prepare/head/ray/rollout/status before the next full run."
    child_capsule: null
    evidence: ["E4", "E5", "E7"]
  - id: "ws_benchmark_evidence"
    name: "Benchmark and evidence packaging"
    owner: "hkang + agents"
    status: "active"
    priority: "high"
    goal: "Produce a credible no-TA vs TA comparison centered on 1-step total runtime."
    latest_truth: "The canonical baseline is Harbor + SkyRL fully-async without a ThunderAgent gateway; the treatment adds the ThunderAgent gateway on the same experiment shape."
    blockers:
      - "No trusted paired artifact set exists yet for baseline and treatment on the current cluster."
      - "Timing and monitoring collection still needs to be standardized across both sides."
    next_action: "Run comparable baseline and treatment captures only after the stage-based path passes a smoke test."
    child_capsule: null
    evidence: ["E1", "E6", "E7"]

facts:
  - id: "F1"
    kind: "fact"
    statement: "This branch is explicitly scoped around the R2EGYM + Harbor + fully-async + ThunderAgent + 6-node + 1-step runtime use case."
    verified_at: "2026-03-22T01:09:55-07:00"
    evidence: ["E1"]
  - id: "F2"
    kind: "fact"
    statement: "The repo contains ThunderAgent-aware Harbor fully-async entrypoints."
    verified_at: "2026-03-22T01:09:55-07:00"
    evidence: ["E2"]
  - id: "F3"
    kind: "fact"
    statement: "ThunderAgent-specific weight-sync behavior is recognized as a first-class integration concern and has a documented coordination design."
    verified_at: "2026-03-22T01:09:55-07:00"
    evidence: ["E3"]
  - id: "F4"
    kind: "fact"
    statement: "A custom cross-job Harbor wrapper now provides stage-oriented lifecycle commands and targeted cleanup-stage behavior."
    verified_at: "2026-03-22T01:09:55-07:00"
    evidence: ["E4"]
  - id: "F5"
    kind: "inference"
    statement: "The main project risk has shifted from missing integration code to unstable experimental orchestration and inconsistent evidence capture."
    verified_at: "2026-03-22T01:09:55-07:00"
    evidence: ["E1", "E2", "E3", "E4", "E5"]
  - id: "F6"
    kind: "stale"
    statement: "A rollout-side problem should default to full-stack restart."
    superseded_by: "F4"

decisions:
  - id: "D1"
    decision: "Use Harbor + SkyRL + fully-async + R2EGYM as the proving ground for ThunderAgent value."
    rationale: "This path exercises the rollout gateway, trainer coordination, and runtime overheads that matter for ThunderAgent."
    rejected_alternatives:
      - "Generic repo-wide benchmark: rejected because it would blur the value of the ThunderAgent gateway."
    impacts:
      - "Most near-term project state lives under examples/train_integrations/harbor."
  - id: "D2"
    decision: "Use 1 training step total wall-clock as the primary near-term benchmark metric."
    rationale: "It is the fastest trustworthy signal for whether the ThunderAgent gateway improves the current training use case."
    rejected_alternatives:
      - "Final accuracy first: rejected because orchestration stability is not strong enough yet for long-run conclusions."
    impacts:
      - "Benchmark artifacts must preserve stage timing and comparable launch conditions."
  - id: "D3"
    decision: "Optimize orchestration for minimal failure cost and stage-local recovery."
    rationale: "The user explicitly values time savings and wants failures to avoid forcing whole-run restarts."
    rejected_alternatives:
      - "Single monolithic launcher with global teardown: rejected because it wastes time and muddies runtime evidence."
    impacts:
      - "Wrapper and monitoring design should prefer restartable stages and explicit cleanup boundaries."

open_issues:
  - id: "O1"
    severity: "high"
    issue: "A trusted paired artifact set for the no-TA baseline and TA treatment has not been produced yet."
    likely_scope: "Benchmark and evidence packaging"
    unblock_condition: "Both variants complete comparable 1-step runs with the same timing and monitoring package."
    evidence: ["E1", "E6"]
  - id: "O2"
    severity: "high"
    issue: "The custom cross-job wrapper has not been dynamically revalidated after the stage-based lifecycle refactor."
    likely_scope: "Experiment orchestration and monitoring"
    unblock_condition: "Prepare/head/ray/rollout/status pass on the current allocation before the next benchmark launch."
    evidence: ["E4", "E5"]
  - id: "O3"
    severity: "medium"
    issue: "The execution workspace is intentionally dirty because the project-level handoff docs and custom wrapper are still untracked."
    likely_scope: "Handoff clarity and reproducibility"
    unblock_condition: "Either commit or formally designate the current untracked files as the execution-layer source of truth."
    evidence: ["E5"]

next_actions:
  - priority: 1
    action: "Validate the stage-based orchestration path on the current cluster up to rollout readiness before launching another full run."
    owner: "Next agent"
    expected_result: "The custom wrapper can bring up prepare/head/ray/rollout cleanly and report status without forcing global teardown."
    verify_with: ["E4", "E7"]
    if_failed_then: "Stop at the failing stage, update project state or replay notes, and repair only that stage."
  - priority: 2
    action: "Capture a canonical 1-step no-ThunderAgent baseline using the same artifact root and timing package planned for the ThunderAgent run."
    owner: "Next agent"
    expected_result: "A baseline timing artifact exists that can be fairly compared against the TA treatment."
    verify_with: ["E6", "E7"]
    if_failed_then: "Treat the failure as orchestration debt first, not as evidence about ThunderAgent value."
  - priority: 3
    action: "Capture the matching 1-step ThunderAgent treatment and compare total step time against the baseline."
    owner: "Next agent"
    expected_result: "A paired timing comparison exists, with enough evidence to explain any observed runtime difference."
    verify_with: ["E1", "E2", "E3", "E7"]
    if_failed_then: "Preserve artifacts, avoid whole-job cleanup, and update project state or replay notes with the failure boundary and next safe resume point."

evidence_index:
  - id: "E1"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/examples/train_integrations/harbor/docs/runbooks/R2EGYM_QWEN3_32B_6NODE_1STEP_TA_RUNTIME_FULL_HANDOFF.md"
    locator: "Search for 'Goal:', 'Use branch:', and 'Success Criteria'"
    note: "Defines the intended ThunderAgent-enabled R2EGYM Harbor runtime experiment shape."
    freshness: "fresh"
  - id: "E2"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/examples/train_integrations/harbor/entrypoints/main_harbor_thunder_agent_fully_async.py"
    locator: "Search for 'Harbor fully-async experiment that routes inference through ThunderAgent'"
    note: "Shows that the repo has a dedicated ThunderAgent-aware Harbor fully-async entrypoint."
    freshness: "fresh"
  - id: "E3"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/examples/train_integrations/harbor/docs/design/THUNDERAGENT_WEIGHT_SYNC_COORDINATION_DESIGN.md"
    locator: "Search for 'Recommendation: Option A'"
    note: "Documents a TA-specific correctness and coordination concern around weight sync."
    freshness: "fresh"
  - id: "E4"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/examples/train_integrations/harbor/run_r2egym_qwen3_32b_5node_rootless_full_1step_miniswe50_timeout9000_fd131072_cross_job_merged_head_rollout.sh"
    locator: "Search for 'usage()' and 'cleanup-stage'"
    note: "Captures the custom cross-job orchestration layer and its stage-oriented lifecycle interface."
    freshness: "fresh"
  - id: "E5"
    type: "command_output"
    path: "git status --short"
    locator: "Look for '?? docs/agent-handoff/', the untracked cross-job replay runbook, and the untracked cross-job wrapper"
    note: "Confirms that the current execution-layer docs and wrapper are untracked."
    freshness: "fresh"
  - id: "E6"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/examples/train_integrations/harbor/run_r2egym_qwen3_32b_6node_rootless_full_1step_miniswe50_timeout9000_fd131072_blockdockerd_replay_no_thunderagent_nockpt.sh"
    locator: "Search for 'SKYRL_DISABLE_THUNDERAGENT=1'"
    note: "Defines the canonical no-ThunderAgent baseline wrapper for the same experiment family."
    freshness: "fresh"
  - id: "E7"
    type: "file"
    path: "/home/hkang/zthunder_yagent/SkyRL/docs/agent-handoff/workstreams/harbor-ta-benchmark.run.yaml"
    locator: "Read the whole file"
    note: "Structured run spec for the current Harbor benchmark path, including baseline and ThunderAgent treatment commands."
    freshness: "fresh"

stale_context_to_ignore:
  - "Do not assume that a rollout or monitoring issue should trigger full-stack teardown by default."
  - "Do not assume that historical launch assets are automatically sufficient for the current cluster shape."
  - "Do not treat long-run accuracy as the current decision gate for the project."

handoff_prompt:
  read_order:
    - "project"
    - "current_state"
    - "constraints"
    - "active_workstreams"
    - "next_actions"
    - "evidence_index only if needed"
    - "the Harbor benchmark run spec"
  first_move: "Open the Harbor benchmark run spec, then validate the stage-based wrapper path before running another timing experiment."
